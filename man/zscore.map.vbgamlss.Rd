% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Support.R
\name{zscore.map.vbgamlss}
\alias{zscore.map.vbgamlss}
\title{Compute voxel-wise Z-scores from VBGAMLSS coefficients maps}
\usage{
zscore.map.vbgamlss(
  yimage,
  mask,
  family = "SHASH",
  mu_hat,
  sigma_hat = NULL,
  nu_hat = NULL,
  tau_hat = NULL,
  num_cores = NULL
)
}
\arguments{
\item{yimage}{A NIFTI image (or path readable by \code{antsImageRead}) containing the observed data to score.}

\item{mask}{An NIFTI image (or path) defining voxels to evaluate (nonzero entries are used).}

\item{family}{Character scalar naming a \pkg{gamlss.dist} family with an available CDF function \code{p<family>} (for example \code{"NO"}, \code{"SHASH"}). Default is \code{"SHASH"}.}

\item{mu_hat, sigma_hat, nu_hat, tau_hat}{NIFTI images (or paths) with the fitted parameter maps corresponding to the chosen family. Supply only the parameters required by the family.}

\item{num_cores}{Integer number of parallel workers. Defaults to \code{parallelly::availableCores()}.}
}
\value{
An z-score image with voxel-wise z-scores.
}
\description{
Given a subject image and predicted parameter maps for a GAMLSS family, compute zscore at each voxel inside a mask and return a z-score image.
}
